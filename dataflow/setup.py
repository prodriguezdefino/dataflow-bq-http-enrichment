import setuptools

setuptools.setup(
    name='dataflow-bq-http',
    version='0.0.1',
    install_requires=[],
    packages=setuptools.find_packages(),
    include_package_data=True,
    description='Dataflow pipeline for BigQuery to HTTP to BigQuery',
)